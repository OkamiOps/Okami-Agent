.venv/
__pycache__/
*.pyc
.env
*.egg-info/
dist/
build/
.pytest_cache/

# Runtime na RAIZ do repo (dados da instalação, não código).
# IMPORTANTE: comentário em LINHA PRÓPRIA — inline (/agents/ # ...) NÃO funciona no .gitignore.
# agentes criados por `okami agent new` (o exemplo fica em examples/company/agents)
/agents/
# imagens recebidas (vision §6)
/inbox/
workspaces/
# estado runtime: sessões, sinais de persona, memória local (por workspace)
.okami/

# overrides locais (mesclados sobre okami.yaml) — podem conter IP/infra interna, NÃO versionar
okami.local.yaml

# macOS
.DS_Store
